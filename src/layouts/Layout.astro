---
import Analytics from '@vercel/analytics/astro';
import SpeedInsights from '@vercel/speed-insights/astro';
import { stegaClean } from '@sanity/client/stega';
import { perspectiveCookieName } from '@sanity/preview-url-secret/constants';
import SanityVisualEditing from '@/components/SanityVisualEditing';
import DisableDraftMode from '@/components/DisableDraftMode';
import { translations } from '@/i18n/translations';
import { getLangFromUrl, stripLocalePrefix, useTranslations } from '@/i18n/utils';
import Footer from '../components/layout/Footer.astro';
import Header from '../components/layout/Header.astro';
import { fetchSiteConfig } from '@/lib/content/api';
import type { SiteConfig } from '@/types';
import '@/styles/global.css';

interface Props {
	title?: string;
	description?: string;
	canonicalPath?: string;
	image?: string;
	siteConfig?: SiteConfig | null;
}

const locale = getLangFromUrl(Astro.url);
const t = useTranslations(locale);
const shared = translations[locale].shared;
const siteUrl = import.meta.env.SITE_URL || import.meta.env.PUBLIC_SITE_URL || 'https://svetzamalo.cz';
const title = stegaClean(Astro.props.title ?? t('layout.defaultTitle'));
const description = stegaClean(Astro.props.description ?? t('layout.defaultDescription'));
const canonicalRawPath = Astro.props.canonicalPath ?? Astro.url.pathname;
const canonicalUrl = new URL(canonicalRawPath, siteUrl).toString();
const pathWithoutLocale = stripLocalePrefix(canonicalRawPath);
const hreflangCs = new URL(pathWithoutLocale || '/', siteUrl).toString();
const hreflangEn = new URL(`/en${pathWithoutLocale || '/'}`, siteUrl).toString();
const image = Astro.props.image ?? '/images/hero/mauritius-home-hero.jpg';
const siteConfig = Astro.props.siteConfig !== undefined ? Astro.props.siteConfig : await fetchSiteConfig(locale);
const analyticsEnabled = siteConfig?.enableAnalytics === true;
let draftMode = false;
try { draftMode = Astro.cookies.has(perspectiveCookieName); } catch { /* not available on prerendered pages */ }
const ogLocale = shared.ogLocale;
---

<!doctype html>
<html lang={locale} class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="generator" content={Astro.generator} />
		<meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1" />
		<meta name="googlebot" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1" />
		<meta name="theme-color" content="#0F6CBD" />
		<meta name="author" content={t('layout.author')} />
		<meta name="seznam-wmt" content="ZV2mM9koaFElO3a0gZudPRjCNidhFJyX" />
		<title>{title}</title>
		<meta name="description" content={description} />
		<link rel="canonical" href={canonicalUrl} />
		<link rel="alternate" hreflang="cs" href={hreflangCs} />
		<link rel="alternate" hreflang="en" href={hreflangEn} />
		<link rel="alternate" hreflang="x-default" href={hreflangCs} />
		<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
		<link rel="icon" type="image/png" sizes="192x192" href="/icons/icon-192.png" />
		<link rel="icon" type="image/png" sizes="512x512" href="/icons/icon-512.png" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" type="image/x-icon" href="/favicon.ico" />
		<link rel="manifest" href="/manifest.json" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playpen+Sans:wght@400;600;700&family=Poppins:wght@600;700&display=swap" />
		<meta property="og:type" content="website" />
		<meta property="og:site_name" content="Svet za malo" />
		<meta property="og:locale" content={ogLocale} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:url" content={canonicalUrl} />
		<meta property="og:image" content={new URL(image, siteUrl).toString()} />
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={new URL(image, siteUrl).toString()} />
		{analyticsEnabled && <Analytics />}
		<slot name="head" />
	</head>
	<body class="flex min-h-screen flex-col antialiased">
		<Header siteConfig={siteConfig} />
		<main id="main-content" class="flex-1">
			<slot />
		</main>
		<Footer siteConfig={siteConfig} />
		{analyticsEnabled && <SpeedInsights />}
		{draftMode && <SanityVisualEditing client:only="react" />}
		{draftMode && <DisableDraftMode client:only="react" />}
	</body>
</html>
